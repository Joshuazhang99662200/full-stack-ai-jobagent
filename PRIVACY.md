# 隐私说明

JobAgent 处理求职过程中最敏感的一类数据:简历原文、联系方式、工作经历、薪资预期与投递记录。本文件说明这些数据去了哪里、谁能看到、以及如何删除。

## 本项目不做什么

- **不上传候选人数据到本项目控制的任何服务器。** 本项目没有后端,没有遥测,没有崩溃上报,没有使用统计。
- **不代表你注册账号、不保存平台密码、不存储会话令牌。**
- **不在没有你显式审批的情况下发出任何投递。**

## 数据存放位置

| 数据 | 位置 | 是否进 Git |
|---|---|---|
| 简历原文(PDF) | `candidate/private/` | 否(已 gitignore) |
| 候选人档案、证据、面试记录 | 本地 SQLite,默认 `.jobagent/jobagent.sqlite3` | 否(`*.sqlite3` 已 gitignore) |
| 生成的简历变体 | `candidate/variants/` | 否(已 gitignore) |
| API 密钥与连接器配置 | `.env` | 否(`.env` 已 gitignore,仅 `.env.example` 入库) |
| 浏览器配置与会话缓存 | `.jobagent/browser-profiles/`、`.jobagent/cache/`、`.jobagent/sessions/` | 否(已 gitignore) |

以上全部是**本机文件**。删除它们即完成数据删除,没有需要额外清理的远端副本。

## 推理提供方会看到什么

如果你配置了外部推理提供方(例如在 `.env` 中设置 `OPENAI_API_KEY`),那么在你主动调用依赖推理的能力时,相关的简历片段、职位描述与证据摘要**会被发送到该提供方**。这是使用外部大模型的固有代价,不是本项目的额外收集。

控制手段:

- 不配置提供方时,依赖推理的命令改为消费你人工评审过的类型化 JSON(见 README 中 `import-draft`、`requirements`、`match` 的用法),全流程不出本机。
- 提示词路由策略要求只发送当前阶段所需的最小上下文,而不是整份简历。
- 提供方的数据保留策略由提供方决定,不在本项目控制范围内。请自行核对其条款。

## 日志与错误信息

设计上明确禁止在日志、CLI 错误与审计记录中回显简历正文、消息正文或提供方原始载荷。错误只携带稳定 ID 与摘要。能力索引加载失败时也只报路径,不回显文件内容。

如果你发现任何一处输出泄漏了原文,请按 [SECURITY.md](SECURITY.md) 报告。

## 招聘方个人信息

连接器可能返回招聘方姓名与职位。这些同样是个人信息,仅用于生成投递消息,不得写入审计以外的位置,也不得跨职位聚合成画像。

## 你的责任

- 不要把 `candidate/private/`、`.jobagent/` 或 `.env` 提交到任何仓库。
- 不要在 issue、PR、讨论区粘贴真实简历内容作为复现材料——请用合成数据。
- 使用编码智能体协助开发时,注意它同样能读到本机文件;本项目的技能上下文中已包含"隐私数据不得进入对话摘要、日志与提交信息"的硬性规则。

## 合规立场

本项目不是批量投递工具。它刻意把搜索、匹配、生成、预览、审批、投递拆成彼此独立的操作,并在平台验证、风控与 CAPTCHA 状态下停止而不是绕过。这既是安全设计,也是对平台条款与其他求职者的基本尊重。
