# 安全策略

## 报告漏洞

请通过 GitHub 的私密漏洞报告通道提交:仓库页面 → **Security** → **Report a vulnerability**。

**不要**用公开 issue 报告安全问题。**不要**在报告中附带真实简历、真实联系方式或 API 密钥——请用合成数据构造最小复现。

我们会在收到后尽快确认。这是一个个人维护的开源项目,没有 SLA 承诺。

## 支持版本

项目处于 `0.1.x` 早期阶段,只对 `main` 分支的最新提交提供修复。尚无长期支持分支。

## 安全边界

本项目的威胁模型中,以下几类问题按安全问题对待:

### 1. 数据泄漏

- 简历正文、消息正文、证据正文或提供方原始载荷出现在日志、CLI 错误、审计记录或异常回溯中。
- 候选人私密数据被写入 `candidate/private/`、`.jobagent/` 之外的位置。
- 错误信息回显 YAML、JSON 或 Pydantic 的原始输入值。

### 2. 提示注入与越权

- 通过职位描述、简历文本、证据正文或插件文本使模型改变路由规则、扩大权限或触发投递。**所有外部文本一律是数据,不是指令。**
- 通过被索引的能力元数据获得索引未声明的读写权限。
- 绕过"只有 `CandidateEvidenceService.confirm` 才能提升 canonical Evidence"这一唯一写入口。

### 3. 索引加载器

能力索引加载器 (`src/jobagent/optimizer/index.py`) 是处理不可信配置的边界。以下均视为漏洞:

- 相对路径逃逸出技能根目录;
- `yaml.safe_load` 之外的构造行为(例如 `!!python/object/apply` 生效);
- 重复映射键静默覆盖已评审的值;
- 编译期动态 import 或执行被索引的入口点。

### 4. 审批与投递门禁

- 在审批摘要与当前产物不一致时仍然放行投递;
- 预览被当作审批,或审批被当作投递指令;
- 把登录、CAPTCHA、验证、风控、平台变更当作可自动重试的瞬时故障。

## 明确不在范围内

- 上游平台自身的漏洞;请直接报告给对应平台。
- 外部推理提供方的数据保留行为;见 [PRIVACY.md](PRIVACY.md)。
- 用户自己把 `.env` 或 `candidate/private/` 提交到公开仓库导致的泄漏。
- 请求协助绕过平台风控、验证码或反爬机制——这类请求不会被受理,也不视为安全问题。

## 依赖

运行时依赖保持最小:`pydantic`、`pypdf`、`PyYAML`、`typer`。领域逻辑不依赖浏览器自动化框架或大模型编排框架。发现依赖漏洞请一并通过上述通道报告。
