interface:
  display_name: "Human-approved Job Hunting"
  short_description: "Evidence-grounded job search and resume workflows"
  default_prompt: "Use $job-hunting to review this job opportunity with evidence and human approval gates."
policy:
  allow_implicit_invocation: true
