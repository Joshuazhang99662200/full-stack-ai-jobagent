# Optimizer evidence contract

Each substantive bullet returns its evidence IDs, requirement IDs, source resume item IDs, rewrite operations, and confidence. Extract claims after drafting and verify each claim only against its cited evidence.

Valid verification states are `SUPPORTED`, `PARTIALLY_SUPPORTED`, `UNSUPPORTED`, and `CONTRADICTED`. Partially supported content must be narrowed and reverified before inclusion.
